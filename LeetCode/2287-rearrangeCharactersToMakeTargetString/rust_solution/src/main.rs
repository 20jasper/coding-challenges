fn main() {
    println!("Hello, world!");
		println!("{}", rearrange_characters('s'.to_string(), 'h'.to_string()))
}

use std::collections::HashMap;

pub fn rearrange_characters(s: String, target: String) -> i32 {
	let mut hash_map = HashMap::new();
	hash_map.insert("jimk", "jim");


	return 10;
}