//URL--
// https://leetcode.com/problems/binary-tree-right-side-view/

//INSTRUCTIONS--
/* 
*/

//SOLUTION--
/* 

*/
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number[]}
 */
const rightSideView = function (root) {
	//go down the the rightmost portion of the tree and add each value to an array
	//count how deep it goes
	//go down the left half 
	//when it's deeper than the right depth, then starting pushing those values to the array
	//return the array
};
//TESTCASES--
console.log();